/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: [
      "img.icons8.com",
      "encurtador.com.br",
      "abrir.link"
    ]
  }
};

export default nextConfig;
